name: Code check

on: [push, pull_request]

jobs:
  lint:
    name: Typescript and test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install dependencies
        run: npm install
      - name: Type check
        run: npm run typecheck
      - name: Tests
        run: npm run test
